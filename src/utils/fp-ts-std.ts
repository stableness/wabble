export {
    function as F,
    string as Str,
    readonlyArray as A,
    either as E,
    option as O,
    predicate as P,
    tuple as Tp,
    number as Num,
    url as URL,
    taskEither as TE,
} from 'fp-ts-std';

export * from 'fp-ts-std';

